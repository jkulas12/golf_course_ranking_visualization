Occasionally when clicking on a course on the list, the charts on left dont update. Using arrow keys
    Always seems to remedy this. Solution is probably making the code to handle the two events the same



On Firefox the map tiles occasionally disappear



* Clicking on a course will bring back a plethora of previously selected courses... probably related to Dr. Staskos earlier comment

* When selecting a group of courses from the list on the right, they highlight on map but do not stay highlightd


* Remove "The's"

* Remove data loading issue

* Some rankings dont have complete data

* sort mode changes when a new ranking is selected or deselected

* when the map starts small and resized map tiles only populate a small portion
